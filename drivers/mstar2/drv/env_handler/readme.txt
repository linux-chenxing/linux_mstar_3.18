env_handler driver is used to access MPOOL partition without using MPOOL partition device node.

config path:
Mstar Platform Configuration
        Env handler         (ps:enter the envirenment variable partition name, default is MPOOL)