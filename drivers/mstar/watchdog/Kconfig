config MS_WATCHDOG
        tristate "watchdog driver"
	depends on ARCH_INFINITY
        help
          Say Y here to enable the driver for the watchdog.

          If unsure, say Y.

          To compile this driver as a module, choose M here: the
          module will be called mdrv_wdt.
